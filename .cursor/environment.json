{
  "name": "agora-protocol-engine",
  "user": "ubuntu",
  "install": "python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && pip install -r requirements.txt && python -m playwright install chromium"
}
